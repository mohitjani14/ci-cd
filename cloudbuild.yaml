# cloudbuild.yaml
steps:
# Step 1: Run unit tests
- name: 'python:3.9-slim-buster'
  entrypoint: 'bash'
  args:
    - '-c'
    - |
      cd app
      pip install -r requirements.txt
      pytest
  id: 'Run Unit Tests'
  dir: 'app' # Set working directory for this step

# Step 2: Build the Docker image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'us-central1-docker.pkg.dev/$PROJECT_ID/my-repo/my-app:$COMMIT_SHA', '.']
  id: 'Build Docker Image'

# Step 3: Push the Docker image to Artifact Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'us-central1-docker.pkg.dev/$PROJECT_ID/my-repo/my-app:$COMMIT_SHA']
  id: 'Push to Artifact Registry'

# Step 4: Deploy the image to Cloud Run
- name: 'gcr.io/google.com/cloudsdk/cloud-sdk'
  args:
    - 'gcloud'
    - 'run'
    - 'deploy'
    - 'my-gcp-cicd-app' # Service name
    - '--image'
    - 'us-central1-docker.pkg.dev/$PROJECT_ID/my-repo/my-app:$COMMIT_SHA'
    - '--region'
    - 'us-central1'
    - '--platform'
    - 'managed'
    - '--allow-unauthenticated' # Allow public access for this example
  id: 'Deploy to Cloud Run'

images:
- 'us-central1-docker.pkg.dev/$PROJECT_ID/my-repo/my-app:$COMMIT_SHA'

options:
  # REQUIRED: Specify how logs should be handled when a service account is used
  logging: CLOUD_LOGGING_ONLY
  # Optional: Increase build timeout if your build is complex
  # timeout: '1200s' # 20 minutes
  # Optional: Use a more powerful machine for faster builds
  # machineType: 'N1_HIGHCPU_8'
